{
  "name": "streamverse-basic-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "./deploy.sh",
    "deploy:vercel": "npm run build && vercel --prod",
    "deploy:dev": "npm run build && vercel"
  },
  "dependencies": {
    "streamverse": "file:../../packages/streamverse"
  },
  "devDependencies": {
    "vite": "^5.3.1",
    "vercel": "^45.0.9"
  }
}
